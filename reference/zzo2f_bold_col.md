# Bold specific columns (Typst backend)

Bold specific columns (Typst backend)

## Usage

``` r
zzo2f_bold_col(cols)
```

## Arguments

- cols:

  Integer or character vector.

## Value

A `zzo2f_format` spec.

## Examples

``` r
if (FALSE) { # \dontrun{
zzo2f_bold_col(c("estimate", "p_value"))
} # }
```
