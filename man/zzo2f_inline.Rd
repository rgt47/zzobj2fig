% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzo2f-inline.R
\name{zzo2f_inline}
\alias{zzo2f_inline}
\title{Render a table inline for R Markdown / Quarto (Typst backend)}
\usage{
zzo2f_inline(
  x,
  width = NULL,
  align = c("center", "left", "right"),
  filename = NULL,
  format = c("auto", "pdf", "png"),
  dpi = 150L,
  sub_dir = NULL,
  caption = NULL,
  label = NULL,
  ...
)
}
\arguments{
\item{x}{A data frame, matrix, or table.}

\item{width}{Character or NULL. Passed through to later include
helpers; \code{zzo2f()} itself does not consume it.}

\item{align}{One of \code{"center"}, \code{"left"}, \code{"right"}.}

\item{filename}{Output base filename; generated if NULL.}

\item{format}{One of \code{"auto"}, \code{"pdf"}, \code{"png"}.
\code{"auto"} picks \code{"pdf"} for LaTeX knitr output and
\code{"png"} otherwise.}

\item{dpi}{PNG resolution.}

\item{sub_dir}{Output directory; \code{tempdir()} if NULL.}

\item{caption}{Character or NULL. Passed to \code{zzo2f()}.}

\item{label}{Character or NULL. Typst label (no angle brackets);
appended to the emitted image reference when running under
knitr.}

\item{...}{Additional arguments forwarded to \code{zzo2f()}.}
}
\value{
Character path to the rendered file (invisible when used
inside knitr).
}
\description{
Generates a PDF or PNG via \code{zzo2f()} and, when
running inside a knitr document, emits the appropriate markdown
image reference so the image is embedded.
}
\examples{
\dontrun{
zzo2f_inline(mtcars[1:5, 1:3], caption = "Demo")
}
}
