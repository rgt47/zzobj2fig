# Resolve column spec to integer indices

Resolve column spec to integer indices

## Usage

``` r
resolve_cols(cols, data)
```

## Arguments

- cols:

  Integer or character vector, or NULL.

- data:

  Data frame.

## Value

Integer vector of column indices, or NULL when `cols` is NULL.
