% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzo2f-formatting.R
\name{zzo2f_format}
\alias{zzo2f_format}
\title{Create a Typst formatting specification}
\usage{
zzo2f_format(
  rows = NULL,
  cols = NULL,
  bold = FALSE,
  italic = FALSE,
  color = NULL,
  background = NULL,
  condition = NULL
)
}
\arguments{
\item{rows}{Integer vector or NULL. Row indices (1-based, data rows
only; header is row 0 in tinytable conventions but is not
targeted here).}

\item{cols}{Integer or character vector or NULL. Column indices or
names.}

\item{bold}{Logical.}

\item{italic}{Logical.}

\item{color}{Character or NULL. LaTeX color name or hex string;
translated via \code{translate_latex_color()}.}

\item{background}{Character or NULL. LaTeX color name or hex
string for cell background.}

\item{condition}{Function or NULL. Applied to cell values (coerced
to character); formatting applies only where it returns TRUE.}
}
\value{
An S3 object of class \code{"zzo2f_format"}.
}
\description{
Define formatting rules for specific cells, rows, or
columns. Consumed by \code{zzo2f()} via its \code{formats}
parameter.
}
\examples{
\dontrun{
zzo2f_format(rows = 1:3, cols = 1, bold = TRUE)
zzo2f_format(cols = "p_value",
             condition = function(x) as.numeric(x) < 0.05,
             bold = TRUE, color = "red")
}
}
