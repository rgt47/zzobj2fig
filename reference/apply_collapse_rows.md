# Apply collapse rows to kable object

Apply collapse rows to kable object

## Usage

``` r
apply_collapse_rows(kable_obj, collapse_spec)
```

## Arguments

- kable_obj:

  A kable object.

- collapse_spec:

  A o2f_collapse object.

## Value

Modified kable object with collapsed rows.
