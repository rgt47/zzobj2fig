# Get the cache directory

Returns the path to the o2f cache directory. Creates it if it doesn't
exist.

## Usage

``` r
o2f_cache_dir(create = TRUE)
```

## Arguments

- create:

  Logical. Create the directory if it doesn't exist.

## Value

Path to the cache directory.
