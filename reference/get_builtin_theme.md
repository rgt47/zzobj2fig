# Get a theme by name

Look up a theme by name, checking both built-in themes and registered
custom themes.

## Usage

``` r
get_builtin_theme(name)
```

## Arguments

- name:

  Character string. Name of the theme.

## Value

A o2f_theme object.
