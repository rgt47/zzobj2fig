# Set the cache directory

Set the cache directory

## Usage

``` r
o2f_cache_set_dir(path)
```

## Arguments

- path:

  Path to use for caching. Set to NULL to use the default (temp
  directory).

## Value

Invisibly returns the previous cache directory.
