# Typst Post-Processing Pipeline

Post-processes the .typ file generated by tinytable before Typst
compilation. Adds features that tinytable does not natively support:
page sizing, font family, caption placement, separator lines, and
compact footnotes.
