% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzo2f-refs.R
\name{zzo2f_ref}
\alias{zzo2f_ref}
\title{Cross-reference to a labeled table (Typst backend)}
\usage{
zzo2f_ref(label, supplement = NULL, cat = TRUE)
}
\arguments{
\item{label}{Character. The label name, without angle brackets.}

\item{supplement}{Character or NULL. Optional supplement text
overriding Typst's default (e.g., "Table", "Figure").}

\item{cat}{Logical. If TRUE (default), print the string via
\code{cat()} for inline use in R Markdown; otherwise return it.}
}
\value{
Character string of the form \code{@label} (or with a
supplement, \code{#ref(<label>, supplement: [Table])}).
}
\description{
Construct a Typst reference string for a labeled
element. The returned string can be pasted into surrounding
prose in a \code{.typ} document. Typst has no direct analog of
LaTeX's \code{\\pageref} or \code{\\nameref}; those LaTeX-only
types are not supported here.
}
\examples{
\dontrun{
zzo2f_ref("tab-demographics", cat = FALSE)
}
}
