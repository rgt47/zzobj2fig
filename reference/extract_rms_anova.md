# Extract ANOVA-style output from rms objects

Internal helper to extract ANOVA (chunk test) results from rms model
objects.

## Usage

``` r
extract_rms_anova(x, digits = 3)
```

## Arguments

- x:

  An rms model object.

- digits:

  Number of decimal places.

## Value

A data frame with ANOVA results.
