# Log messages if verbose is TRUE

Log messages if verbose is TRUE

## Usage

``` r
log_message(msg, verbose = FALSE)
```

## Arguments

- msg:

  A message to display.

- verbose:

  Logical indicating whether to display the message. Defaults to FALSE.
