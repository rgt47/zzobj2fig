# Get cache statistics

Report on the current state of the o2f cache.

## Usage

``` r
o2f_cache_info()
```

## Value

A list with cache statistics.

## Examples

``` r
if (FALSE) { # \dontrun{
o2f_cache_info()
} # }
```
